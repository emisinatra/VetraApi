package com.example.vetra.services;

import com.example.vetra.entities.Direccion;

public interface DireccionService extends BaseService<Direccion, Long> {
    // Si necesitamos algo específico para las direcciones que no esté en BaseService, va acá.
}