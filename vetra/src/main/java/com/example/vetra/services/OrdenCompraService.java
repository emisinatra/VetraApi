package com.example.vetra.services;

import com.example.vetra.entities.OrdenCompra;

public interface OrdenCompraService extends BaseService<OrdenCompra, Long> {
    // Para cositas específicas de las órdenes de compra que no cubra BaseService, las ponemos acá.
}